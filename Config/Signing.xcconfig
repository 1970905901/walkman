// 签名配置入口（本文件入库，不含任何个人信息）
//
// 你的 Team ID 写在同目录的 Local.xcconfig 里，该文件已被 .gitignore 忽略：
//
//     DEVELOPMENT_TEAM = ABCDE12345
//
// 复制 Local.xcconfig.example 改名为 Local.xcconfig 即可。
// `#include?` 的问号表示文件不存在时不报错 —— 所以 clone 下来不配置也能打开工程，
// 只是签名那一步会提示要选开发团队。
#include? "Local.xcconfig"
